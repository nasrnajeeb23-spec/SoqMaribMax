# سوق مارب | SouqMarib Open Marketplace

هذا المشروع هو منصة سوق مفتوح متكاملة تم بناؤها باستخدام React، TypeScript، و Tailwind CSS. يتيح للمستخدمين التسجيل كبائعين أو مشترين، عرض السلع، إتمام عمليات الشراء، وطلب خدمات التوصيل.

## ✨ الحالة الحالية

التطبيق الآن يعمل ببنية **Client/Server** حقيقية مع **قاعدة بيانات دائمة** وجاهز للانطلاق الكامل.
- **الواجهة الأمامية (Frontend):** جاهزة للنشر على **Vercel**.
- **الواجهة الخلفية (Backend):** متصلة بقاعدة بيانات PostgreSQL وجاهزة للنشر على **Render**.

---

## 🚀 النشر على الإنترنت (الخطوات النهائية)

### الخطوة 1: إعداد قاعدة البيانات على Render (مرة واحدة فقط)

1.  **إنشاء قاعدة بيانات جديدة:**
    - في لوحة تحكم Render، اضغط على **"New +"** ثم اختر **"PostgreSQL"**.
    - أعطها اسماً (مثلاً `souqmarib-db`)، وتأكد من اختيار الخطة **`Free`**.
    - اضغط **"Create Database"** وانتظر حتى تكتمل وتصبح حالتها "Available".

2.  **ربط قاعدة البيانات بالخادم:**
    - اذهب إلى صفحة قاعدة البيانات الجديدة، وابحث عن قسم **"Connections"**.
    - انسخ الرابط الموجود تحت عنوان **"Internal Connection String"**.
    - الآن اذهب إلى خدمة الخادم الخلفي (`souqmarib-backend`).
    - اذهب إلى تبويب **"Environment"**.
    - في قسم "Environment Variables"، أضف متغيراً جديداً:
        - **Key:** `DATABASE_URL`
        - **Value:** الصق الرابط الذي نسخته.
    - اضغط **"Save Changes"**. سيقوم Render بإعادة تشغيل الخادم تلقائياً.

3.  **تأسيس جداول قاعدة البيانات:**
    - اذهب إلى صفحة قاعدة البيانات مرة أخرى.
    - اذهب إلى تبويب **"Shell"** (أو قد يكون اسمه **"PSQL"**).
    - سيفتح لك نافذة أوامر. **انسخ كل محتويات ملف `schema.sql` الموجود في المشروع والصقها في هذه النافذة**، ثم اضغط Enter.
    - يجب أن ترى رسائل مثل `CREATE TABLE` و `INSERT 0 1` تظهر. هذا يؤكد أنه تم إنشاء الجداول وإدخال البيانات بنجاح.

### الخطوة 2: نشر الخادم الخلفي (Backend)

- **الحالة:** الخادم الآن جاهز للنشر مع إعدادات قاعدة البيانات الجديدة.
- **الخطوات:**
  1. اذهب إلى خدمة الخادم الخلفي (`souqmarib-backend`) على Render.
  2. اضغط على زر **"Manual Deploy"**.
  3. اختر **"Deploy latest commit"**.

### الخطوة 3: نشر الواجهة الأمامية (Frontend)

- **الحالة:** هذه هي الخطوة الأخيرة لإطلاق تطبيقك.
- **الخطوات:**
  1.  اذهب إلى حسابك على **Vercel** وأنشئ مشروعاً جديداً.
  2.  اربط مستودع GitHub الخاص بمشروع "سوق مارب".
  3.  سيقوم Vercel تلقائياً بالتعرف على المشروع كـ **Vite Project**. **لا تقم بتغيير أي شيء.**
  4.  اضغط على **"Deploy"**.

بعد اكتمال النشر، سيعطيك Vercel رابطاً عاماً لمشروعك. **هذا هو رابط تطبيقك النهائي على الإنترنت!**

---

## 💻 التشغيل المحلي (للتطوير)

لتشغيل التطبيق بالكامل على جهازك، يجب أن يكون لديك PostgreSQL مثبت ويعمل محلياً.

1.  **أنشئ قاعدة بيانات محلية** وأحصل على رابط الاتصال الخاص بها.
2.  **أنشئ ملف `.env`** في جذر المشروع وأضف رابط قاعدة البيانات:
    ```
    DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE
    ```
3.  **قم بتشغيل `schema.sql`** على قاعدة بياناتك المحلية.
4.  **شغل الخادم:** `npm start`
5.  **في نافذة أخرى، شغل الواجهة:** `npm run dev`
