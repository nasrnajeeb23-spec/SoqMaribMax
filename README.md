# سوق مارب | SouqMarib Open Marketplace

هذا المشروع هو منصة سوق مفتوح متكاملة تم بناؤها باستخدام React، TypeScript، و Tailwind CSS. يتيح للمستخدمين التسجيل كبائعين أو مشترين، عرض السلع، إتمام عمليات الشراء، وطلب خدمات التوصيل.

## ✨ الحالة الحالية

التطبيق الآن يعمل ببنية **Client/Server** حقيقية:
- **الواجهة الأمامية (Frontend):** جاهزة للنشر على **Vercel**.
- **الواجهة الخلفية (Backend):** جاهزة للنشر على **Render**.

## 🛠️ التقنيات المستخدمة

- **React.js**
- **TypeScript**
- **Vite**
- **Zustand** (لإدارة الحالة)
- **Tailwind CSS**
- **Node.js / Express.js** (للخادم الخلفي)

---

## 💻 التشغيل المحلي (للتطوير)

لتشغيل التطبيق بالكامل على جهازك، يجب تشغيل الخادم والواجهة معاً في نافذتين منفصلتين.

1.  **تثبيت الاعتماديات (مرة واحدة):**
    ```bash
    npm install
    ```

2.  **تشغيل الخادم الخلفي (Backend):**
    - في نافذة الأوامر الأولى، قم بتشغيل:
      ```bash
      npm start
      ```
    - يجب أن ترى رسالة `Server is running on port 4000`. اترك هذه النافذة مفتوحة.

3.  **تشغيل الواجهة الأمامية (Frontend):**
    - في نافذة أوامر **جديدة**، قم بتشغيل:
      ```bash
      npm run dev
      ```
    - سيفتح هذا الأمر التطبيق في متصفحك وسيكون متصلاً بالخادم المحلي.

---

## 🚀 النشر على الإنترنت (خطوات نهائية)

### 1. نشر الواجهة الخلفية (على Render)

- **الحالة:** لقد قمت بنشر الخادم بنجاح على الرابط: `https://soqmaribmax.onrender.com`
- **ملاحظة:** إذا قمت بإجراء أي تغييرات على ملف `backend-server.js` أو `package.json`، يجب عليك الذهاب إلى لوحة تحكم Render والقيام بـ **"Manual Deploy"** لتطبيق التغييرات.

### 2. نشر الواجهة الأمامية (على Vercel)

- **الحالة:** هذه هي الخطوة الأخيرة لإطلاق تطبيقك.
- **الخطوات:**
  1.  اذهب إلى حسابك على **Vercel** وأنشئ مشروعاً جديداً.
  2.  اربط مستودع GitHub الخاص بمشروع "سوق مارب".
  3.  سيقوم Vercel تلقائياً بالتعرف على المشروع كـ **Vite Project** وتعيين الإعدادات الصحيحة. **لا تقم بتغيير أي شيء.**
  4.  اضغط على **"Deploy"**.

بعد اكتمال النشر، سيعطيك Vercel رابطاً عاماً لمشروعك. **هذا هو رابط تطبيقك النهائي على الإنترنت!**