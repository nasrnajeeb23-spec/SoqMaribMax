# سوق مارب | SouqMarib Open Marketplace

هذا المشروع هو منصة سوق مفتوح متكاملة تم بناؤها باستخدام React، TypeScript، و Tailwind CSS. يتيح للمستخدمين التسجيل كبائعين أو مشترين، عرض السلع، إتمام عمليات الشراء، وطلب خدمات التوصيل.

## ✨ الميزات الرئيسية

- **بنية Client/Server:** تم فصل التطبيق إلى واجهة أمامية (Frontend) وخادم خلفي (Backend).
- **نظام أدوار متكامل:** بائع، مشتري، ومسؤول.
- **لوحات تحكم مخصصة:** لكل دور لوحة تحكم خاصة به.
- **بحث وتصفية متقدمة:** سهولة العثور على المنتجات.
- **نظام توصيل ومدفوعات:** دورة شراء كاملة من عرض المنتج إلى طلبه وتوصيله.
- **نظام إشعارات فوري:** (محاكاة باستخدام BroadcastChannel).
- **واجهة عصرية ومتجاوبة:** تصميم احترافي يعمل على جميع الأجهزة.

## 🛠️ التقنيات المستخدمة

### الواجهة الأمامية (Frontend)

- **React.js:** لمكتبة واجهة المستخدم.
- **TypeScript:** للتحقق من الأنواع وجعل الكود أكثر قوة.
- **Tailwind CSS:** للإطار العمل الخاص بالتصميم.
- **React Router:** لإدارة التنقل داخل التطبيق.
- **Zustand:** لإدارة الحالة المركزية.

### الواجهة الخلفية (Backend)

- **Node.js:** لبيئة تشغيل الخادم.
- **Express.js:** لإطار العمل الخاص ببناء الـ API.
- **CORS:** للسماح للواجهة الأمامية بالاتصال بالخادم.

## 🚀 التشغيل والنشر (Deployment)

لتحويل هذا المشروع من مجرد كود إلى تطبيق حي على الإنترنت، اتبع الخطوات التالية بالترتيب.

### الخطوة 1: تجهيز المشروع ورفعه على GitHub

إذا لم تكن قد فعلت ذلك بالفعل، يجب رفع جميع ملفات المشروع إلى مستودع (repository) خاص بك على [GitHub](https://github.com/).

### الخطوة 2: نشر الواجهة الخلفية (Backend) على Render

1.  **إنشاء مجلد للخلفية:** في مشروعك على جهازك، قم بإنشاء مجلد جديد باسم `backend`.
2.  **نقل الملفات:** انقل ملفي `backend-server.js` و `backend-package.json` إلى داخل مجلد `backend` الذي أنشأته.
3.  **رفع التغييرات:** قم برفع هذه التغييرات (المجلد الجديد وملفاته) إلى مستودعك على GitHub.
4.  **التسجيل في Render:** اذهب إلى [dashboard.render.com](https://dashboard.render.com/) وأنشئ "Web Service" جديد.
5.  **ربط المستودع:** اربط حساب GitHub الخاص بك واختر المستودع الذي يحتوي على مشروعك.
6.  **إعدادات الخدمة:**
    - **Name:** اختر اسماً لخدمتك (مثال: `souqmarib-backend`).
    - **Root Directory:** أدخل `backend` (اسم المجلد الذي أنشأناه).
    - **Environment:** اختر `Node`.
    - **Build Command:** `npm install`.
    - **Start Command:** `npm start`.
7.  **النشر:** اضغط على "Create Web Service". سيقوم Render ببناء ونشر الخادم. قد يستغرق هذا بضع دقائق.
8.  **نسخ الرابط:** بعد انتهاء النشر بنجاح، سيمنحك Render رابطاً عاماً لخادمك (مثال: `https://souqmarib-backend.onrender.com`). **انسخ هذا الرابط، ستحتاجه في الخطوة التالية.**

### الخطوة 3: تعديل ونشر الواجهة الأمامية (Frontend) على Vercel

1.  **تحديث رابط الـ API:**
    - افتح ملف `api/index.ts` في مشروعك.
    - ستجد في الأعلى متغيراً باسم `API_URL`.
    - **استبدل القيمة `'https://YOUR_RENDER_BACKEND_URL.onrender.com'` بالرابط الذي نسخته من Render في الخطوة السابقة.**
    - احفظ الملف وقم برفع هذا التغيير إلى مستودعك على GitHub.
2.  **التسجيل في Vercel:** اذهب إلى [vercel.com](https://vercel.com/) وأنشئ "Project" جديد.
3.  **ربط المستودع:** اربط حساب GitHub الخاص بك واختر نفس المستودع.
4.  **إعدادات المشروع:**
    - **Framework Preset:** اختر `Other`.
    - **Build and Output Settings:** **لا تقم بتغيير أي شيء.** اترك إعدادات البناء فارغة.
5.  **النشر:** اضغط على "Deploy". سيقوم Vercel بنشر واجهة تطبيقك.
6.  **الوصول للتطبيق:** سيعطيك Vercel رابطاً عاماً لمشروعك. هذا هو رابط تطبيقك النهائي!

**تهانينا! تطبيقك الآن يعمل بشكل كامل على الإنترنت.**
